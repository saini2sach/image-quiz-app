{
  "name": "image-quiz-app",
  "version": "0.1.0",
  "description": "This application provides an Quiz-interface in which user have to select correct answer for the displayed image in stipulated time-frames for winning.",
  "private": true,
  "homepage": "https://saini2sach.github.io/image-quiz-app/",
  "dependencies": {
    "gh-pages": "^2.0.1",
    "react": "^16.5.2",
    "react-dom": "^16.5.2",
    "react-scripts": "^3.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "braces": ">=2.3.1",
    "enzyme": "^3.10.0",
    "eslint": ">=4.18.2",
    "js-yaml": ">=3.13.1",
    "lodash": ">=4.17.13",
    "mem": ">=4.0.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
